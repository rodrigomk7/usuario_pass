spring:
  application:
    name: message-v2
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: dev

management:
  endpoints:
    web:
      exposure:
        include: refresh